# Ce dossier contient les fichiers de modèles ML entraînés (.pkl, .h5, .onnx, etc.)
# Chaque modèle est référencé en base de données avec ses métadonnées (version, précision, date)
# Ne pas supprimer manuellement — utiliser l'interface ou l'API
