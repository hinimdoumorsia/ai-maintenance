# Ce dossier contient les fichiers bruts uploadés par les utilisateurs (CSV, Excel, JSON)
# Les fichiers sont stockés localement et référencés en base de données (SQLite)
# Ne pas supprimer manuellement — utiliser l'interface ou l'API DELETE /api/donnees/datasets/{id}
